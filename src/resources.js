const minecraftData = require('minecraft-data');
// Blocks, Items, Foods, Mobs, Enchantments, Entities

const mcData = minecraftData('1.20');
console.log(`Minecraft Version ${mcData.version}: Data Loaded!`);
